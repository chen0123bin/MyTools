using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestHotfixMono : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LWDebug.Log("55555");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
